general:
  branches:
    ignore:
      - ggmaster
      - hots
      - jonomon
      - master
      - old_master

dependencies:
    post:
        - pip install tox tox-pyenv
        - pyenv local  2.7.11 3.5.1
